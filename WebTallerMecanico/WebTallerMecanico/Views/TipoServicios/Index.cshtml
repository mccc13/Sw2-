@model IEnumerable<WebTallerMecanico.Models.TipoServicio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Servicio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Servicio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.tipoServiciosID }) |
            @Html.ActionLink("Details", "Details", new { id=item.tipoServiciosID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.tipoServiciosID })
        </td>
    </tr>
}

</table>
